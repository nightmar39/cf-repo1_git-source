apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: codefresh-delivery-pipelines
spec:
  rules:
    - http:
        paths:
          - path: /webhooks/proj
            backend:
              service:
                name: proj-eventsource-svc
                port:
                  number: 80
            pathType: ImplementationSpecific
